version: "3"

services:
  fronend:
    build:
      context: .
      dockerfile: Dockerfile                     
    image: ${DOCKER_IMAGE_NAME:-hediaminfrontimg}:${TAG:-latest}
    container_name: ${DOCKER_CONTAINER_NAME:-hediaminContainer}
    ports:
      - ${APP_PORT:-3001}:3001    
    restart: unless-stopped

