version: "3"

services:
  fronend:
    build:
      context: .
      dockerfile: Dockerfile                     
    image: ${DOCKER_IMAGE_NAME:-aminhediifrontimg}:${TAG:-latest}
    container_name: ${DOCKER_CONTAINER_NAME:-aminHediiContainer}
    ports:
      - ${APP_PORT:-3000}:3000    
    restart: unless-stopped

